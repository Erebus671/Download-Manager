name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Configure GitHubPackages credentials
        shell: pwsh
        run: >-
          dotnet nuget update source GitHubPackages
          --username Erebus671
          --password ${{ secrets.GH_PACKAGES_READ_TOKEN }}
          --store-password-in-clear-text
          --configfile nuget.config

      - name: Restore
        run: >-
          dotnet restore DownloadManagerApplet.sln
          --source https://api.nuget.org/v3/index.json
          --source https://nuget.pkg.github.com/Erebus671/index.json

      - name: Build
        run: dotnet build DownloadManagerApplet.sln -c Release --no-restore

      - name: Test
        run: >-
          dotnet test DownloadManagerApplet.sln -c Release --no-build
          --logger "trx;LogFileName=test-results.trx"
          --results-directory TestResults

      - name: Publish test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: TestResults/**/*.trx
