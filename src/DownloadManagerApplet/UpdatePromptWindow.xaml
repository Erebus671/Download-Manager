<Window x:Class="DownloadManagerApplet.UpdatePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AtraTech Download Solutions"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner" SizeToContent="WidthAndHeight"
        Background="{StaticResource PanelBrush}"
        FontFamily="Segoe UI" FontSize="13">

    <Border BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="1" Padding="20">
        <StackPanel Width="440">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <Image x:Name="TitleIcon" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center"
                       RenderOptions.BitmapScalingMode="HighQuality" Visibility="Collapsed" />
                <TextBlock x:Name="HeadingText" FontWeight="SemiBold" FontSize="15"
                           Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" />
            </StackPanel>

            <TextBlock Foreground="{StaticResource TextDimBrush}" Margin="0,0,0,10">
                <Run x:Name="DetailRun" /><Run Text=" &#183; " /><Run Text="&#x2713; Signature verified" Foreground="{StaticResource OkBrush}" />
            </TextBlock>

            <Border Background="{StaticResource PanelAltBrush}" BorderBrush="{StaticResource AppBorderBrush}"
                    BorderThickness="1" Padding="10,8">
                <StackPanel>
                    <TextBlock Text="What's new" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,4" />
                    <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="NotesText" Foreground="{StaticResource TextBrush}" TextWrapping="Wrap" />
                    </ScrollViewer>
                </StackPanel>
            </Border>

            <StackPanel x:Name="ActivePanel" Orientation="Horizontal" Margin="0,12,0,0" Visibility="Collapsed">
                <Border Style="{StaticResource StatusPill}" Margin="0,0,8,0">
                    <TextBlock x:Name="ActiveText" FontSize="11" Foreground="{StaticResource WarnBrush}" />
                </Border>
                <TextBlock x:Name="ActiveNoteText" Foreground="{StaticResource TextDimBrush}" VerticalAlignment="Center" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button AutomationProperties.AutomationId="UpdateSkipButton" Content="Skip this version"
                        Click="Skip_Click" Margin="0,0,8,0" />
                <Button AutomationProperties.AutomationId="UpdateLaterButton" Content="Later" IsCancel="True"
                        Click="Later_Click" Margin="0,0,8,0" MinWidth="72" />
                <Button AutomationProperties.AutomationId="UpdateInstallButton" Content="Install and restart" IsDefault="True"
                        Style="{StaticResource PrimaryButton}" Click="Install_Click" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
